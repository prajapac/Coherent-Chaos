const assert = require('chai').assert;

describe('Common Utilities', () => {
    describe('utility 1', () => {
        it('should have certain behaviour', ()=> {
            assert(true);
        });
    });
});
